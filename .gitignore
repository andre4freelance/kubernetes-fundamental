# Local Claude Code project instructions — memuat detail cluster/prod, JANGAN dipublikasikan
CLAUDE.md

# Kubeconfig & credential — JANGAN pernah di-commit ke repo publik
*.config
*.kubeconfig
kubeconfig*
*kubeconfig*
.kube/
*.pem
*.key
*token*
